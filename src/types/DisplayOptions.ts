// type to track which of the various panes are popped out in the WeatherApplication
export type DisplayOptions = {
  dateBox: boolean;
  weatherBox: boolean;

  // toolbars
  seasonBar: boolean;
  biomeBar: boolean;
}
